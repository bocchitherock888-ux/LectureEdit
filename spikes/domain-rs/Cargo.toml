[package]
name = "lectureedit-merge"
version = "0.1.0"
edition = "2021"
description = "G0 spike: conservative three-way merge ported from reference/core.py"
license = "MIT"
publish = false

[dependencies]
difflib = "0.4"
regex = "1"
once_cell = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
